on: [push]

jobs:
  docker:
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
    - name: Setup
      run: sudo make build
    
    - name: Test
      run: sudo make test

    - name: Teardown
      run: sudo make down

