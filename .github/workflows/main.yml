on: [push]

jobs:
  docker:
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
    - name: Setup
      run: make build
    
    - name: Test
      run: make test

    - name: Teardown
      run: make down

